<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - SoniqAI</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@200;300;400;500;600;700;800;900&family=Space+Grotesk:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700;800;900&family=IBM+Plex+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --gold-primary: #C9A961;
            --gold-accent: #E4C988;
            --gold-bright: #F2E7A7;
            --gold-dark: #B8985A;
            --navy-deep: #0B0D17;
            --navy-medium: #151922;
            --navy-light: #1F2332;
            --purple-dark: #1A0B2E;
            --purple-medium: #2D1B4E;
            --black-rich: #000000;
            --white-pure: #FFFFFF;
            --white-soft: #F8F9FA;
            --gray-light: #8892A6;
            --gray-medium: #5C6B7D;
            --gray-dark: #404854;
            --surface-glass: rgba(255, 255, 255, 0.02);
            --surface-glass-hover: rgba(255, 255, 255, 0.05);
            --surface-glass-strong: rgba(255, 255, 255, 0.08);
            --border-glass: rgba(201, 169, 97, 0.12);
            --border-glass-hover: rgba(201, 169, 97, 0.25);
            --border-glass-strong: rgba(201, 169, 97, 0.35);
            --error-color: #EF4444;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Outfit', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--navy-deep);
            color: var(--white-pure);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }

        /* Background System */
        .background-system {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            z-index: -2;
            background: linear-gradient(135deg, var(--navy-deep) 0%, var(--purple-dark) 50%, var(--navy-medium) 100%);
        }

        .mesh-gradient {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            z-index: -1;
            background: 
                radial-gradient(circle at 20% 80%, rgba(201, 169, 97, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(45, 27, 78, 0.15) 0%, transparent 50%);
            animation: meshFloat 20s ease-in-out infinite;
        }

        /* Auth Container */
        .auth-container {
            width: 100%;
            max-width: 480px;
            margin: 0 auto;
            padding: 40px;
            background: var(--surface-glass-strong);
            border: 1px solid var(--border-glass);
            border-radius: 24px;
            backdrop-filter: blur(24px);
            box-shadow: 0 32px 80px rgba(0, 0, 0, 0.3);
            position: relative;
        }

        .auth-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .auth-logo {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 32px;
            font-weight: 700;
            background: linear-gradient(135deg, var(--gold-primary) 0%, var(--gold-bright) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.02em;
            margin-bottom: 16px;
        }

        .auth-title {
            font-family: 'Playfair Display', serif;
            font-size: 28px;
            font-weight: 600;
            margin-bottom: 12px;
            color: var(--white-pure);
        }

        .auth-subtitle {
            font-size: 16px;
            color: var(--gray-light);
            font-weight: 300;
        }

        .auth-form {
            display: flex;
            flex-direction: column;
            gap: 24px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .form-label {
            font-size: 14px;
            font-weight: 500;
            color: var(--gold-accent);
        }

        .form-input {
            background: var(--surface-glass);
            border: 1px solid var(--border-glass);
            border-radius: 12px;
            padding: 16px 20px;
            font-size: 16px;
            color: var(--white-pure);
            font-family: inherit;
            transition: all 0.3s ease;
            backdrop-filter: blur(12px);
        }

        .form-input:focus {
            outline: none;
            border-color: var(--gold-primary);
            box-shadow: 0 0 0 3px rgba(201, 169, 97, 0.1);
            background: var(--surface-glass-hover);
        }

        .form-input::placeholder {
            color: var(--gray-medium);
        }

        .form-help {
            font-size: 12px;
            color: var(--gray-medium);
            margin-top: 4px;
        }

        .plan-selector {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            margin-top: 8px;
        }

        .plan-option {
            background: var(--surface-glass);
            border: 1px solid var(--border-glass);
            border-radius: 12px;
            padding: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
        }

        .plan-option:hover {
            background: var(--surface-glass-hover);
            border-color: var(--border-glass-hover);
        }

        .plan-option.selected {
            border-color: var(--gold-primary);
            background: rgba(201, 169, 97, 0.1);
        }

        .plan-title {
            font-size: 14px;
            font-weight: 600;
            color: var(--white-pure);
            margin-bottom: 4px;
        }

        .plan-description {
            font-size: 12px;
            color: var(--gray-light);
        }

        .auth-button {
            background: linear-gradient(135deg, var(--gold-primary), var(--gold-accent));
            color: var(--black-rich);
            padding: 16px 32px;
            border-radius: 12px;
            border: none;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.23, 1, 0.320, 1);
            box-shadow: 0 12px 40px rgba(201, 169, 97, 0.25);
            position: relative;
            overflow: hidden;
        }

        .auth-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 16px 48px rgba(201, 169, 97, 0.35);
        }

        .auth-button:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .auth-link {
            text-align: center;
            margin-top: 24px;
        }

        .auth-link a {
            color: var(--gold-accent);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .auth-link a:hover {
            color: var(--gold-primary);
        }

        .error-message {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.2);
            border-radius: 8px;
            padding: 12px;
            color: var(--error-color);
            font-size: 14px;
            margin-bottom: 24px;
            text-align: center;
        }

        .back-link {
            position: absolute;
            top: 20px;
            left: 20px;
            color: var(--gray-light);
            text-decoration: none;
            font-size: 14px;
            transition: color 0.3s ease;
        }

        .back-link:hover {
            color: var(--gold-accent);
        }

        /* Animations */
        @keyframes meshFloat {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(20px, -20px) rotate(1deg); }
            66% { transform: translate(-20px, 10px) rotate(-1deg); }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .auth-container {
                margin: 20px;
                padding: 32px 24px;
            }
            
            .plan-selector {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Background System -->
    <div class="background-system"></div>
    <div class="mesh-gradient"></div>

    <a href="/" class="back-link">← Back to Home</a>

    <div class="auth-container">
        <div class="auth-header">
            <div class="auth-logo">SoniqAI</div>
            <h1 class="auth-title">Create Your Account</h1>
            <p class="auth-subtitle">Join the manifestation music revolution</p>
        </div>

        <% if (error) { %>
            <div class="error-message">
                <%= error %>
            </div>
        <% } %>

        <form class="auth-form" method="POST" action="/signup">
            <div class="form-group">
                <label class="form-label" for="name">Full Name</label>
                <input type="text" id="name" name="name" class="form-input" placeholder="Enter your full name" required>
            </div>

            <div class="form-group">
                <label class="form-label" for="email">Email Address</label>
                <input type="email" id="email" name="email" class="form-input" placeholder="Enter your email address" required>
            </div>

            <div class="form-group">
                <label class="form-label" for="password">Password</label>
                <input type="password" id="password" name="password" class="form-input" placeholder="Create a strong password" required minlength="6">
                <div class="form-help">Minimum 6 characters</div>
            </div>

            <div class="form-group">
                <label class="form-label" for="oneTimeCode">Access Code</label>
                <input type="text" id="oneTimeCode" name="oneTimeCode" class="form-input" placeholder="Enter your one-time access code" required maxlength="12" style="text-transform: uppercase;">
                <div class="form-help">12-digit access code provided by administrator</div>
            </div>

            <button type="submit" class="auth-button">Create Account</button>
        </form>

        <div class="auth-link">
            Already have an account? <a href="/login">Sign In</a>
        </div>
    </div>

    <script>
        // Auto-uppercase the access code
        document.getElementById('oneTimeCode').addEventListener('input', function(e) {
            e.target.value = e.target.value.toUpperCase();
        });

        // Form validation
        document.querySelector('.auth-form').addEventListener('submit', function(e) {
            const password = document.getElementById('password').value;
            const oneTimeCode = document.getElementById('oneTimeCode').value;

            if (password.length < 6) {
                e.preventDefault();
                alert('Password must be at least 6 characters long');
                return;
            }

            if (oneTimeCode.length !== 12) {
                e.preventDefault();
                alert('Access code must be exactly 12 characters. Current length: ' + oneTimeCode.length);
                return;
            }

            // Check if code contains only alphanumeric characters
            if (!/^[A-Z0-9]{12}$/.test(oneTimeCode)) {
                e.preventDefault();
                alert('Access code must contain only uppercase letters and numbers');
                return;
            }
        });

        // Smooth form animations
        const inputs = document.querySelectorAll('.form-input');
        inputs.forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.style.transform = 'translateY(-2px)';
            });
            
            input.addEventListener('blur', function() {
                this.parentElement.style.transform = 'translateY(0)';
            });
        });
    </script>
</body>
</html>